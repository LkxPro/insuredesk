# Copy to apps/api/.env and fill in real values for local development.
# The API validates these with Zod at startup (see src/env.ts); a missing or
# malformed value crashes the process. Prisma also reads this file.

# Postgres connection string used by Prisma and the API.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Dev: matches docker-compose.yml postgres credentials
DATABASE_URL="postgresql://insuredesk:insuredesk_dev@localhost:5432/insuredesk?schema=public"

# API server binding
HOST="0.0.0.0"
PORT="3000"

# One of: development | test | production
NODE_ENV="development"

# pino log level: fatal | error | warn | info | debug | trace | silent
LOG_LEVEL="info"

# Session secret for signing cookies (change in production!)
SESSION_SECRET="change-me-in-production-use-a-long-random-string"

# Session max age in seconds (default: 24 hours)
SESSION_MAX_AGE_SECONDS="86400"
