# 客服工单系统（InsureDesk）

保险行业客服工单管理系统：统一受理多渠道客户投诉/咨询/理赔请求，按投诉等级驱动 SLA，跟踪工单全生命周期。本文件只是领域词汇表——完整规则与字段见 `PRD.md`，关键取舍见 `docs/adr/`。

## Language

### 工单与生命周期

**Ticket（工单）**：
一条客户服务请求（投诉/咨询/理赔等）的记录，系统核心实体。生命周期：unassigned → assigned → processing → completed（字段见 PRD §3.1，流转见 PRD §4.1）。

**workOrderNumber（工单号）**：
人念人查的业务把手：`WO` + 全局 DB 序列（如 WO100001），不含年月、允许断号（PRD §9.3）。
_Avoid_: 拿 id 示人——id 是不透明主键，只作外键/URL 引用

**Channel（反馈渠道）**：
工单所属的业务渠道，固定 4 种：保司/经纪/支付/监管。排班与自动分配按它匹配。可不填（#43）：未填渠道的工单仍可手动分配，但按排班自动分配会跳过并说明原因。
_Avoid_: 与 source 混用——source 是录入方式（manual/feishu_form/community），兼作"由谁创建"的判别器

**Status（工单状态）**：
数据库只存 4 个基础状态（unassigned/assigned/processing/completed）；pending_timeout 与 overdue 是**计算状态**，查询时按 dueAt 实时算、仅覆盖显示（ADR 0001）。状态只能经生命周期动作流转，不可直接编辑；completed 是终态，不可重开。

**CompletionStatus（完结状态）**：
完结时必选的原因，12 种封闭枚举、无"其他"（全列表见 PRD §9.1）。

**ProcessLog（处理记录）**：
工单操作的审计日志，构成详情页时间线，记录"当时发生的事实"（7 种 action、from/to 约定与生成规则见 PRD §3.2）。只服务追溯与展示，不承担考核统计（考核走 assigneeId，ADR 0003）。

**Attachment（附件）**：
工单处理过程中上传的文件材料。

**软删除**：
删除工单 = 设 deletedAt，默认列表与一切统计排除，ProcessLog/附件保留；本期只删不恢复（PRD §4.5）。

### SLA 与提醒

**ComplaintLevel（投诉等级）**：
一般/高级/加急/特急投诉，4 级。**唯一驱动 SLA 的字段**——决定首响时限、跟进频次、超时时长与提醒规则（各级规则见 PRD §4.2，管理员可配置）。可不填（未定级，#43）：dueAt/首响要求/跟进频次全空，不产生 SLA 时间类告警；后续编辑补级时，SLA 仍以原始 createdAt 起算。
_Avoid_: priority（它不驱动任何 SLA）

**Priority（优先级）**：
独立的自由标签（low/medium/high/urgent），默认空、手动赋值，仅供人工标注与排序参考，与 SLA 无关。

**SLA 时钟**：
一切 SLA 计时（首响/跟进检查点/超时）自 **createdAt（录入时刻）** 起算，与是否分配无关——未分配的工单也在计时、也会超时，有意倒逼尽快分配（ADR 0002）。

**dueAt（处理时限）**：
创建时一次算定：createdAt + 等级对应时长（一般/高级 48h、加急 72h、特急不设），分配/改派永不重算（ADR 0002）；改 complaintLevel 时按新等级重算。

**首响（First Response）**：
首次实际电话联系客户 = 工单第一条 comment，无独立字段。firstResponseMinutes 只作"待首响"告警的染红阈值，不入考核（PRD §3.8）。

**SLAPolicy（SLA 策略）**：
按投诉等级各一条的可配置规则：首响违约线 + 超时时长 + 类型化提醒规则列表（follow_up_checkpoint / rolling_follow_up 两种；字段与各级默认值见 PRD §3.8，取舍见 ADR 0005）。

**收件箱（AppNotification）**：
通知两轨制之轨 1：用户操作触发的真事件（本期仅 assigned），同步落库，有已读/未读与 toast（ADR 0004）。

**我的待办**：
通知两轨制之轨 2：待首响/检查点未达/特急欠跟进/due_soon/overdue 等时间类告警，**不落库**——前端 30 秒轮询时按当前 assigneeId 名下工单读时计算；未分配工单不进任何人的待办（ADR 0004）。

**超时口径**：
两个视角**有意不同**：看板"已超时数"= 实时运营视角（在途且已过 dueAt，完结即移出）；考核"超时单数"= 历史追责视角（曾超时即计入，含超时完结）。见 ADR 0003。

### 人与权限

**User（用户）/ Role（角色）**：
角色 = 权限点集合（权限点全表见 PRD §5.1）；数据范围仅两档：全部/个人（PRD §5.2）。User.team 是纯描述标签，不驱动任何权限或过滤。

**Assignee（责任人）**：
工单当前的处理人，同一时间只有一个，可改派。分配是主管/管理员的职责——一线客服看不到未分配池，不能自行认领。

**Follower（跟进人）**：
即当前责任人，无独立字段——读时由 assigneeId JOIN users 派生；历史交接链看 ProcessLog 时间线。
_Avoid_: 落库的 follower 字段或姓名快照（改名后陈旧）

**跟进人考核**：
3 个维度——完单数 / 平均完结时长（completionTime − createdAt，端到端口径）/ 超时单数，全部按**当前 assigneeId** 归属，不追踪转单历史（ADR 0003）。

**Schedule（排班）**：
按日期/班次/渠道配置的值班安排，**只**是"按排班自动分配"的候选人来源（算法见 PRD §4.3）。班次 2 种：早班 09:00-18:00 / 晚班 12:00-21:00。手动分配/批量分配/改派与排班相互独立——可指向任意启用用户，不看是否当班、班次或渠道（PRD §4.3，#42）；唯一人选约束是用户处于启用状态。
_Avoid_: 在手动分配路径上加"是否在岗"校验（那是自动分配专属的候选人过滤）
